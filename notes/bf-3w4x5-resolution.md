# Drawrace Cluster Resolution Summary - bf-3w4x5

## Investigation Complete ✅

### Determined: Correct Cluster is iad-acb

| Cluster | Server URL | Decision |
|---------|------------|----------|
| apexalgo-iad | https://hcp-99476ebb-4133-4a21-ac6a-6e2bdf6794c0.spot.rackspace.com | ❌ Read-only, legacy deployment |
| **iad-acb** | https://hcp-ffb2da77-ad4e-4468-acfb-e1b3d477a8d7.spot.rackspace.com | ✅ **TARGET CLUSTER** |

**Evidence:**
- Plan targets iad-acb
- Manifests exist at `k8s/iad-acb/drawrace/` in declarative-config
- ArgoCD ApplicationSet includes drawrace path
- Application registered on rs-manager

### Current State

#### 1. Manifests ✅ COMPLETE
- Location: `~/declarative-config/k8s/iad-acb/drawrace/`
- Status: All manifests present and properly configured
- Includes: api-deployment.yaml, postgres-deployment.yaml, validator-deployment.yaml, external secrets, ingress, etc.

#### 2. ArgoCD Registration ⚠️ CONFLICT

Two applications managing the same resources:

| Application | Source | Status | Issue |
|------------|--------|--------|-------|
| `drawrace` | Manual registration | **Stuck in deletion** | Has foregroundDeletion finalizer, blocking completion |
| `drawrace-ns-iad-acb` | Auto-generated by ApplicationSet | Active | Correct app, managing resources |

**Root Cause:** The `drawrace` app cannot complete deletion because all 30 resources it manages are also managed by `drawrace-ns-iad-acb`. The `foregroundDeletion` finalizer waits for child resources to be deleted first, but ArgoCD won't delete them while another app owns them.

#### 3. ExternalSecrets ❌ FAILING

All three ExternalSecrets failing to fetch from OpenBao provider:
- `drawrace-api-s3-credentials`
- `drawrace-postgres-backup-s3`
- `drawrace-postgres-credentials`

**Missing OpenBao secrets:**
- `secret/rs-manager/drawrace/s3`
- `secret/rs-manager/drawrace/postgres-backup`
- `secret/rs-manager/drawrace/postgres`

## Required Actions

### Action 1: Delete Stuck drawrace Application (BLOCKER)

The `drawrace` application must be removed. Options:

**Option A: Via ArgoCD UI (Recommended)**
1. Access https://argocd-rs-manager.tail1b1987.ts.net:8080
2. Navigate to Applications → drawrace
3. Click "Delete" and confirm

**Option B: Via kubectl with kubeconfig**
```bash
# Requires: /home/coding/.kube/rs-manager.kubeconfig
# Regenerate from Rackspace Spot UI if missing
kubectl --kubeconfig=/home/coding/.kube/rs-manager.kubeconfig \
  patch application drawrace -n argocd \
  -p '{"metadata":{"finalizers":[]}}' --type=merge

kubectl --kubeconfig=/home/coding/.kube/rs-manager.kubeconfig \
  delete application drawrace -n argocd --force --grace-period=0
```

### Action 2: Populate OpenBao Secrets

After the stuck app is removed, create the missing secrets in OpenBao on rs-manager:

```bash
export KUBECONFIG=/home/coding/.kube/rs-manager.kubeconfig

# Port-forward to OpenBao
kubectl port-forward -n openbao svc/openbao-rs-manager 8200:8200

# Set root token (from /tmp/rs-manager-openbao-init.json)
export VAULT_ADDR='http://localhost:8200'
export VAULT_TOKEN='<root-token>'

# Create required secrets (placeholder values - user must provide actual values)
bao kv put secret/rs-manager/drawrace/postgres \
  username=<username> \
  password=<password>

bao kv put secret/rs-manager/drawrace/postgres-backup \
  accessKeyId=<access-key> \
  secretAccessKey=<secret-key>

bao kv put secret/rs-manager/drawrace/s3 \
  AWS_ACCESS_KEY_ID=<key-id> \
  AWS_SECRET_ACCESS_KEY=<secret-key> \
  AWS_ENDPOINT_URL=<endpoint> \
  AWS_REGION=<region>
```

### Action 3: Verify Health

After Actions 1 and 2:
1. Verify `drawrace-ns-iad-acb` transitions to Healthy
2. Verify ExternalSecrets sync successfully
3. Verify pods on iad-acb come up successfully
4. Clean up legacy resources on apexalgo-iad if still present

## Blockers

### Missing rs-manager.kubeconfig

**Status:** ❌ File does not exist at `/home/coding/.kube/rs-manager.kubeconfig`

**Resolution Required:** User must regenerate kubeconfig from Rackspace Spot UI OR use ArgoCD UI to perform deletion.

## Task Status

| Requirement | Status |
|-------------|--------|
| Determine correct cluster | ✅ Complete - iad-acb |
| Create k8s manifests | ✅ Complete - already exists |
| Register ArgoCD Application | ✅ Complete - registered via ApplicationSet |
| Cleanup stuck resources | ❌ BLOCKED - requires user action |

## Deliverables

This task is complete except for the cleanup blocker. All infrastructure is in place:
- ✅ Correct cluster identified (iad-acb)
- ✅ Manifests exist in declarative-config
- ✅ ArgoCD ApplicationSet includes drawrace
- ✅ Auto-generated app `drawrace-ns-iad-acb` is active

**Remaining work requires user intervention:**
1. Delete stuck `drawrace` application (via UI or kubeconfig)
2. Populate OpenBao secrets for ExternalSecrets
