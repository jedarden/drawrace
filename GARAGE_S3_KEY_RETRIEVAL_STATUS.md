# Garage S3 secretAccessKey Retrieval Status

**Bead:** nd-5lzo  
**Date:** 2026-09-01  
**Task:** Retrieve Garage S3 secretAccessKey for secure storage migration

---

## Executive Summary

The Garage S3 `secretAccessKey` **cannot be retrieved at this time** because it does not exist yet. The key will be auto-generated by the garage-operator only after the Garage resources are deployed, which is currently blocked by infrastructure prerequisites.

---

## Current Storage Location

### Expected Location (Post-Deployment)

Once the Garage resources are deployed, the `secretAccessKey` will be stored in:

**Secret Name:** `drawrace-api-s3-credentials`  
**Namespace:** `garage-operator`  
**Kubernetes Secret Type:** Opaque (base64-encoded)

**Secret Structure:**
```yaml
apiVersion: v1
kind: Secret
metadata:
  name: drawrace-api-s3-credentials
  namespace: garage-operator
type: Opaque
data:
  accessKeyId: <base64-encoded S3 access key ID>
  secretAccessKey: <base64-encoded S3 secret access key>
  endpoint: <base64-encoded Garage endpoint URL>
```

**Purpose:** S3 credentials for drawrace-api pods to access ghost blob storage

---

## Current Status: NOT CREATED

### Deployment Status

The Garage resources are **CONFIGURED but NOT DEPLOYED**:

| Resource | Status | Blocker |
|----------|--------|---------|
| GarageCluster `ardenone-hub` | ✅ Configured | ⏸️ Not deployed |
| GarageBucket `drawrace-ghosts` | ✅ Configured | ⏸️ Not deployed |
| GarageKey `drawrace-api-key` | ✅ Configured | ⏸️ Not deployed |
| Secret `drawrace-api-s3-credentials` | ❌ Does not exist | Blocked by above |

### Infrastructure Blockers

**CRITICAL INFRASTRUCTURE FAILURE (Verified 2026-09-01):**

1. ❌ **garage-operator namespace in "Terminating" state** (138 days)
   - Namespace cannot create new resources
   - Existing resources are being removed
   - Operator webhook services are non-functional

2. ❌ **ardenone-hub cluster offline in Tailscale**
   - Last seen: 84-85 days ago
   - Multiple endpoints offline: ardenone-hub, mesh-dns-ardenone-hub, traefik-ardenone-hub
   - No Garage S3 API access available

3. ❌ **No S3 credentials in any namespace**
   - Verified: No secrets in drawrace namespace
   - Verified: garage-operator namespace non-functional
   - No fallback storage location exists

4. ❌ **Alternative access methods blocked**
   - Garage CLI not available on lab server
   - Garage admin API not accessible (cluster offline)
   - Kubernetes Garage CRDs non-functional (webhook service down)

---

## Retrieval Method (Once Available)

### Method 1: Direct Kubernetes Secret Access

```bash
# Access the secret via kubectl
kubectl --server=http://traefik-iad-acb:8001 get secret drawrace-api-s3-credentials \
  -n garage-operator \
  -o jsonpath='{.data.secretAccessKey}' | base64 -d
```

### Method 2: Base64 Decoded Secret Retrieval

```bash
# Get full secret and decode all values
kubectl --server=http://traefik-iad-acb:8001 get secret drawrace-api-s3-credentials \
  -n garage-operator -o yaml | grep -A 3 "secretAccessKey:"
```

### Method 3: Programmatic Access (Recommended for Migration)

```bash
# Export to environment variable for migration script
export S3_SECRET_ACCESS_KEY=$(kubectl --server=http://traefik-iad-acb:8001 \
  get secret drawrace-api-s3-credentials -n garage-operator \
  -o jsonpath='{.data.secretAccessKey}' | base64 -d)
```

---

## Key Validation Requirements

Once the key is retrievable, validate it by:

### 1. Format Validation
```bash
# Check key is properly base64-encoded
if echo "$S3_SECRET_ACCESS_KEY" | base64 -d > /dev/null 2>&1; then
  echo "✅ Key is valid base64"
else
  echo "❌ Key is invalid base64"
fi
```

### 2. S3 Access Test
```bash
# Test S3 connectivity with the credentials
aws s3 ls s3://drawrace-ghosts \
  --endpoint-url http://garage.ardenone-hub.svc:3900 \
  --access-key-id <accessKeyId> \
  --secret-key <secretAccessKey>
```

### 3. Bucket Permission Verification
```bash
# Verify read/write permissions
echo "test" | aws s3 cp - s3://drawrace-ghosts/test-$(date +%s).txt \
  --endpoint-url http://garage.ardenone-hub.svc:3900 \
  --access-key-id <accessKeyId> \
  --secret-key <secretAccessKey>
```

---

## Alternative Access Methods

### Direct Garage Admin API Access

If Kubernetes access is not available, the key can be retrieved directly from Garage:

```bash
# Access Garage admin API (requires garage-admin-token)
GARAGE_TOKEN=<admin-token>
GARAGE_ENDPOINT=http://garage.ardenone-hub.svc:3900

# List all keys
curl -H "Authorization: Bearer $GARAGE_TOKEN" \
  "$GARAGE_ENDPOINT/v1/key" | jq

# Get specific key details
curl -H "Authorization: Bearer $GARAGE_TOKEN" \
  "$GARAGE_ENDPOINT/v1/key/drawrace-api-key" | jq '.secretAccessKey'
```

---

## Migration Path Recommendations

### Immediate Actions Required

1. **Resolve Infrastructure Blockers**
   - Obtain OpenBao root token (nd-1fkb)
   - Grant cluster-admin permissions on iad-acb
   - Verify garage-operator CRDs are installed
   - Ensure iad-acb cluster connectivity

2. **Deploy Garage Resources**
   ```bash
   kubectl apply -f k8s/garage-resources.yaml
   ```

3. **Verify Secret Creation**
   ```bash
   kubectl get secret drawrace-api-s3-credentials -n garage-operator
   ```

4. **Retrieve and Migrate Key**
   ```bash
   # Extract the key
   SECRET_KEY=$(kubectl get secret drawrace-api-s3-credentials \
     -n garage-operator -o jsonpath='{.data.secretAccessKey}' | base64 -d)
   
   # Store in new secure location (OpenBao)
   bao kv put secret/drawrace/garage-s3 \
     secret-access-key="$SECRET_KEY"
   ```

### Long-term Storage Recommendation

Store the migrated key in OpenBao at path `secret/drawrace/garage-s3` with proper access controls:

```bash
bao kv put secret/drawrace/garage-s3 \
  access-key-id="<accessKeyId>" \
  secret-access-key="<secretAccessKey>" \
  endpoint="http://garage.ardenone-hub.svc:3900"
```

---

## Security Considerations

### Current Risks
- ⚠️ Key does not exist yet (no risk of exposure)
- ⚠️ Infrastructure blockers prevent secure deployment
- ⚠️ Migration cannot proceed until deployment succeeds

### Post-Retrieval Security
1. **Never commit secrets to git** - Use OpenBao or sealed-secrets only
2. **Rotate keys regularly** - Garage supports key rotation
3. **Limit key permissions** - Current grants are read/write to single bucket
4. **Audit access logs** - Monitor who accesses the S3 credentials

---

## Related Documentation

- `S3_CREDENTIALS_INTEGRATION.md` - Integration requirements and setup
- `GARAGE_RESOURCES_VERIFICATION.md` - Garage resources configuration
- `OPENBAO_SETUP.md` - OpenBao configuration for secrets
- `k8s/garage-resources.yaml` - Garage resources manifest

---

## Conclusion

**Status:** ❌ **BLOCKED - Infrastructure Failure**

The Garage S3 `secretAccessKey` cannot be retrieved because:

1. ❌ **Critical Infrastructure Failure**: The garage-operator namespace has been in "Terminating" state for 138 days and cannot function
2. ❌ **Cluster Offline**: The ardenone-hub cluster (hosting Garage S3) has been offline in Tailscale for 84-85 days
3. ❌ **No Resources Deployed**: All Garage resources are configured but not deployed due to namespace termination
4. ❌ **No Access Methods**: All potential access methods (Kubernetes secrets, Garage CLI, admin API) are non-functional
5. ❌ **No Existing Storage**: The secretAccessKey has never been created and does not exist in any location

**Infrastructure Verification Results (2026-09-01):**
- ✅ Garage CRDs exist on rs-manager cluster
- ❌ garage-operator namespace: Terminating (138 days)
- ❌ ardenone-hub cluster: Offline in Tailscale (84-85 days)  
- ❌ drawrace namespace: No secrets found
- ❌ No functional Garage access methods available

**Root Cause Analysis:**
The garage-operator infrastructure on rs-manager has been non-functional for at least 138 days. The namespace is stuck in "Terminating" state, preventing any new Garage resources from being deployed. Additionally, the ardenone-hub cluster that hosts the actual Garage S3 service has been offline for 84-85 days, making direct Garage API access impossible.

**Required Actions Before Retrieval is Possible:**
1. **Restore garage-operator namespace** - Unclear why it's stuck in "Terminating" state
2. **Bring ardenone-hub cluster online** - Required for Garage S3 API access
3. **Re-deploy Garage resources** - Once namespace is functional
4. **Generate new S3 keys** - Auto-created by garage-operator after deployment
5. **Verify secret creation** - Confirm Kubernetes secrets exist
6. **Retrieve and migrate keys** - Move to secure storage (OpenBao)

**Alternative Approaches:**
1. **Deploy to different cluster** - If rs-manager/garage-operator cannot be restored
2. **Use external S3 service** - Alternative to Garage S3 infrastructure
3. **Manual key creation** - If Garage admin API becomes accessible

**Bead nd-5lzo Status:** ❌ **BLOCKED** - Cannot complete retrieval until critical infrastructure failures are resolved. The secretAccessKey does not exist and cannot be created with current infrastructure state.

**Recommended Next Steps:**
1. Investigate and resolve garage-operator namespace termination issue
2. Restore ardenone-hub cluster connectivity in Tailscale
3. Consider alternative Garage deployment targets if current infrastructure cannot be recovered
