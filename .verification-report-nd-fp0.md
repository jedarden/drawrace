# Drawrace Pod Verification Report (nd-fp0)

**Date:** 2026-07-02
**Cluster:** iad-acb
**Namespace:** drawrace
**Verification:** After node fix and secret population

## Acceptance Criteria

All pods should show Running/Ready status:
- drawrace-api (2 replicas) — needs drawrace-api-secrets (DATABASE_URL, REDIS_URL, S3_BUCKET) + drawrace-client-key
- drawrace-validator (1 replica, HPA 1-3) — needs drawrace-postgres-credentials + redis
- drawrace-live (2 replicas) — no secrets (room coordination only)
- drawrace-postgres (1 replica) — needs drawrace-postgres-credentials + drawrace-postgres-backup-s3
- redis (1 replica) — no secrets, emptyDir

## Actual Pod Status

| Pod | Replicas | Status | Issue |
|-----|----------|--------|-------|
| drawrace-api | 2 | ImagePullBackOff | Cannot pull images |
| drawrace-validator | 1 | Pending | FailedScheduling: Insufficient CPU |
| drawrace-live | 2 | Pending | FailedScheduling: Insufficient CPU |
| drawrace-postgres | 1 | Init:CreateContainerConfigError | Init container failing |
| redis | 1 | **Running** ✅ | Only healthy pod |

## Blocking Issues

### 1. OpenBao Secret Sync Failed (3/4 ExternalSecrets)

```
NAME                            STATUS              READY
docker-hub-registry             SecretSynced        True  ✅
drawrace-api-s3-credentials     SecretSyncedError   False ❌
drawrace-postgres-backup-s3     SecretSyncedError   False ❌
drawrace-postgres-credentials   SecretSyncedError   False ❌
```

**Error message:** "could not get secret data from provider"

The OpenBao ClusterSecretStore itself is valid and Ready, but individual ExternalSecrets cannot fetch secret data.

### 2. Image Pull Errors

- `drawrace-api` pods in `ImagePullBackOff` despite `docker-hub-registry` secret being synced
- `drawrace-postgres` init container cannot pull `amazon/aws-cli:latest`

### 3. Resource Exhaustion

- Both nodes at **99% CPU allocation** (1490m/1500m and 1497m/1500m)
- Cannot schedule new pods: `drawrace-live` (200m CPU × 2), `drawrace-validator` (200m CPU)

### 4. Node Status

✅ **Both nodes are Ready** - node 2 has successfully recovered

## Root Cause Analysis

The primary blocker is **OpenBao secret population failure**. The prerequisite condition from bead nd-639 is not met:

> "Once iad-acb node 2 is Ready AND OpenBao secrets are populated (ExternalSecrets synced)"

Node 2 is Ready, but OpenBao secrets are NOT synced (3/4 failing).

## Next Steps

1. **Fix OpenBao secret population** (bead nd-639 or equivalent):
   - Investigate why ExternalSecrets cannot fetch from OpenBao
   - Verify secret paths exist in OpenBao
   - Check OpenBao authentication

2. **Address resource constraints**:
   - Add more nodes or reduce pod resource requests
   - Current capacity insufficient for drawrace-live + validator

3. **Retry this verification** (nd-fp0) after secrets are synced

## Conclusion

**VERIFICATION FAILED** ❌

Acceptance criteria not met. Only redis is Running. Bead nd-fp0 should remain open for automatic retry after OpenBao secrets are properly populated.
